{
  "name": "Soccerville (Next.js + Firebase emulators)",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "firebase-emulators",
      "command": "firebase emulators:start --only auth,firestore --project soccerville",
      "description": "Local Firebase Auth (9099) and Firestore (8080) emulators plus the Emulator UI (4000). Required for local development; the app must not run against production Firebase."
    },
    {
      "name": "next-dev",
      "command": "yarn dev",
      "description": "Next.js dev server on http://localhost:3001. Reads .env.local and connects to the Firebase emulators."
    }
  ],
  "ports": [
    { "name": "next-dev", "port": 3001 },
    { "name": "emulator-ui", "port": 4000 },
    { "name": "firestore-emulator", "port": 8080 },
    { "name": "auth-emulator", "port": 9099 }
  ]
}
